@model DynamicLinqWebDocs.Models.Expression

@{
    ViewBag.Title = Model.Name + " Expression Method";
}

@Html.ActionLink("Back to Expressions", "Index", "Expression", null, new { @class = "btn btn-xs btn-default" })

<h2>@Model.Name <small>Expression Method</small></h2>

@Html.FormatMarkdown(Model.Description)

@Html.DisplayFor(x => x.Remarks, "Remarks")
@Html.DisplayFor(x => x.Examples, "Examples")